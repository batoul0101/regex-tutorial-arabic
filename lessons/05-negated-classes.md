# 🚫 **الدرس 5: الفئات المنفية (Negated Character Classes)**

### **رمز النفي `^` داخل الأقواس:**
- عندما يوضع `^` كأول محرف داخل `[ ]`، فإنه **ينفي** المجموعة
- تطابق **أي محرف ليس** من بين المحارف المحددة

### **مثال:**
`[^aeiou]` تطابق أي حرف **ليس** حرف علة
`[^0-9]` تطابق أي حرف **ليس** رقماً
`[^a-z]` تطابق أي حرف **ليس** حرفاً إنجليزياً صغيراً

### **المثال:**
`gr[^ae]y` تطابق `groy`، `grby`، `grcy`... لكنها **لا** تطابق `gray` أو `grey`
