# 🚩 **الدرس 14: الرايات (Flags)**

### **الرايات الشائعة:**
- `i` - حساسية حالة الأحرف (case-insensitive)
- `g` - بحث عام (global) - يبحث عن جميع المطابقات
- `m` - متعدد الأسطر (multiline) - يعامل `^` و `$` كبداية ونهاية كل سطر
- `s` - النقطة تطابق كل المحارف بما فيها newline (dotall)
- `u` - معالجة Unicode كاملة

### **استخدام الرايات:**
في JavaScript: `/pattern/gi`
في Python: `re.compile(pattern, re.IGNORECASE | re.MULTILINE)`

### **المثال:**
`/hello/gi` تطابق `hello`، `Hello`، `HELLO`...
`/^start/m` تطابق `start` في بداية أي سطر
