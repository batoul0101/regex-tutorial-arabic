# 👥 **الدرس 9: المجموعات (Groups)**

### **الأقواس العادية `( )`:**
- تجميع جزء من التعبير النمطي
- تسمح بتطبيق المحددات الكمية على مجموعة كاملة
- **تلتقط** النص المطابق للاستخدام لاحقاً (backreference)

### **المثال:**
`(abc)+` تطابق `abc`، `abcabc`، `abcabcabc`...
`(ab|cd)+` تطابق `ab`، `cd`، `abcd`، `cdab`...

### **التقاط المجموعات:**
يمكن الرجوع إلى المجموعات الملتقطة باستخدام `\1`، `\2`، إلخ
