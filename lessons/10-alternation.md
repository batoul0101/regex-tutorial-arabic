# 🔀 **الدرس 10: التناوب (Alternation)**

### **الرمز `|`:**
- يعني **أو** (بديل)
- يطابق النمط على يساره **أو** النمط على يمينه
- يستخدم غالباً مع الأقواس لتحديد النطاق

### **المثال:**
`cat|dog` تطابق إما `cat` أو `dog`
`gr(a|e)y` تطابق إما `gray` أو `grey` (مثل `gr[ae]y`)
`(Mon|Tues|Wednes|Thurs|Fri|Satur|Sun)day` تطابق أي يوم في الأسبوع

### **الأولوية:**
`|` له أولوية منخفضة، لذا `^abc|def$` تعني: إما `^abc` أو `def$`
