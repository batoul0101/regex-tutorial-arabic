# 📍 **الدرس 11: الرؤوس والتذييلات (Anchors)**

### **رموز تحديد الموضع:**
- `^` - تطابق **بداية** السلسلة (أو بداية السطر مع flag multiline)
- `$` - تطابق **نهاية** السلسلة (أو نهاية السطر مع flag multiline)
- `\b` - تطابق حدود الكلمة (حرف كلمة على جانب، وغير كلمة على الجانب الآخر)
- `\B` - تطابق **ليس** على حدود الكلمة

### **المثال:**
`^Hello` تطابق `Hello` فقط إذا كانت في بداية السلسلة
`world$` تطابق `world` فقط إذا كانت في نهاية السلسلة
`\bword\b` تطابق الكلمة `word` فقط ككلمة منفصلة (وليس كجزء من كلمة أكبر)
