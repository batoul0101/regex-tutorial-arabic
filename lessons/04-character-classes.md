# 🧩 **الدرس 4: فئات المحارف (Character Classes)**

### **الأقواس المربعة `[ ]`:**
- تطابق **أي محرف واحد** من بين المحارف الموجودة داخل الأقواس
- مثال: `[aeiou]` تطابق أي حرف علة إنجليزي

### **نطاق المحارف:**
- يمكن تحديد نطاق باستخدام `-`
- مثال: `[a-z]` تطابق أي حرف صغير، `[A-Z]` تطابق أي حرف كبير
- مثال: `[0-9]` تطابق أي رقم من 0 إلى 9

### **المثال:**
`gr[ae]y` تطابق كل من `gray` و `grey`
`[A-Za-z]` تطابق أي حرف إنجليزي
